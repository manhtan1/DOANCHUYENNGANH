@model IEnumerable<phim2101.Models.Ve>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaPhong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaPhim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaPhong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaPhim)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaVe }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaVe }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaVe })
        </td>
    </tr>
}

</table>

